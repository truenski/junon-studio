---
interface Props {
  code: string;
  language?: string;
}

const { code, language = "text" } = Astro.props;
---

<pre class="bg-muted/50 border border-border rounded-lg p-4 overflow-x-auto">
<code class="text-sm font-mono text-foreground">{code}</code>
</pre>

