---
id: isLoggedIn
name: isLoggedIn
description: true if logged in. false if not
syntax: $isLoggedIn($player)
parameters:
  - name: $player
    type: string
    required: true
    description: The player parameter
returns: true if logged in. false if not
examples:
  - code: $isLoggedIn($player)
    description: Example usage of $isLoggedIn
lang: en
---

true if logged in. false if not
