---
import CodeBlock from "./CodeBlock.astro";

interface Props {
  code: string;
  description?: string;
}

const { code, description } = Astro.props;
---

<div class="mb-4">
  {description && (
    <p class="text-sm text-white/70 mb-2">{description}</p>
  )}
  <CodeBlock code={code} />
</div>

